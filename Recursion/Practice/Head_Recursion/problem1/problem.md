### **Problem 1: Print numbers from 1 to n using head recursion

Write a function that prints numbers from 1 to n, but you must print them in ascending order using head recursion (no loops, and you cannot simply reverse a descending print).

*Hint:* In head recursion, you reach the base case first, then print on returning.
