
### **Problem 2: Find the factorial of a number using head recursion**


Implement factorial such that the recursive call is made before the multiplication.

Example:
`fact(5)` → recursive call to `fact(4)` happens first, then multiplies by 5 on the way back.
